# 반복문 : 제어문에는 크게 조건문, 반복문이 있음. 조건문 if / 반복문 for,while

1. for 반복문
    for 변수 in range(시작값, 종료값, 증가값/감소값)
        처리문
* 종료값에는 +1을 더해줘야만 원하는 횟수에 대한 반복값을 얻을수 있다
* range는 범위를 나타내지만 반복 횟수를 의미하기도 한다
|    for 변수 in range(횟수):
|        print("내용")

* 반복문을 활용할 때는 range 위치에 담겨진 container(list, tuple)을 가져올 수 있다
    num = [11, 22, 42, 15, 33]
    for i in num:
        print(i)

* 반복문과 리스트를 활용할 때는 인덱스를 활용할 수도 있다.: 매우 고급진 코딩 기법
    num = [11, 22, 42, 15, 33]
        for i in range(~):   # range(len(num)) 자동으로 항목의 개수를 파악해서 횟수로 사용
            print(num[i])

2. while 반복문 : 조건이 참인동안 반복하다가 거짓이면 탈출하는 반복문
    1) while(조건값):
        처리문
    2) while(True) : 무조건 반복 구간을 수행하다가 특정한 조건을 만족하면 무한반복 구간을 벗어나는 반복문
        처리문
        break
